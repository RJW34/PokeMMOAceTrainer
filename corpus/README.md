# Corpus directory

Do not commit copyrighted game captures by default. Store manifests, hashes, labels, synthetic fixtures, and transformation scripts here. Keep raw user-owned recordings outside version control or in an explicitly approved private artifact store.

Each corpus entry should record provenance, source mode, redaction status, UI metadata, labels, expected policy output, and train/validation/test split.
